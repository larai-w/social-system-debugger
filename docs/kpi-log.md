# KPI 週次ログ — 週1行、これだけ埋める

> MARKETING.md「数字なき実績は実績にならない」の実体。**毎週日曜の結果スレッド投稿前に1行埋める**。
> 100人到達時の「数字の揃った物語」（就活・出願資料）は、この表がそのまま素材になる。
> 数字が取れない週は「-」でよい。ゼロや小さい数字も正直に書く（後から見ると成長曲線になる）。

## 週次ログ

| 週 | 新規訪問(Web) | インストール | weekly_start | weekly_clear | share_x | share_line | card_saved | 巻き戻し実行 | 一行メモ（何をして何が起きたか） |
|---|---|---|---|---|---|---|---|---|---|
| 2026-W28 |  |  |  |  |  |  |  |  | （記入例）AWS本番公開・エクスポート/教材ページ追加。まだ告知前 |
| 2026-W29 |  |  |  |  |  |  |  |  |  |
| 2026-W30 |  |  |  |  |  |  |  |  |  |

- 数字の出どころ: 新規訪問=アナリティクスの Unique Visitors ／ それ以外=カスタムイベント（下記の有効化が前提）
- インストール: App Store Connect / Play Console の週次
- 月1回、MARKETING.md の KPI ダッシュボード節と見比べて「どの経路が人を連れてくるか」を1行で総括する

## 崩壊モード別の反応比較（W43〜W46）

> design-note-page5.md §4-2 の運用。W43〜W46 は PAGE 5 素材の小出し週で、壊れ方の質感を意図的に対にしている。
> **静かに壊れる系（W43+W44）と 轟音で壊れる系（W45+W46）で、どちらの反応が伸びるか**をここで比較する。
> 判定に使うのは「クリア率（＝難しさより手応え）」と「share系合計（＝拡散したくなったか）」と「X告知のエンゲージ」。
> **X 側の数字（インプレッション・エンゲージ）は track の有効化を待たずに手動で埋められる**ので、☐9 が済むまでは
> weekly_* 列は「-」のまま、X列だけでも記録を始めてよい（後から weekly_* を追記する）。

| 週 | 質感 | weekly_start | weekly_clear | クリア率 | share系合計(x+line+card) | Xインプレ(手動) | Xエンゲージ(手動) |
|---|---|---|---|---|---|---|---|
| 2026-W43 | 静かに壊れる |  |  |  |  |  |  |
| 2026-W44 | 静かに壊れる |  |  |  |  |  |  |
| 2026-W45 | 轟音で壊れる |  |  |  |  |  |  |
| 2026-W46 | 轟音で壊れる |  |  |  |  |  |  |
| **静か系 計（W43+W44）** | — |  |  |  |  |  |  |
| **轟音系 計（W45+W46）** | — |  |  |  |  |  |  |

- クリア率 = weekly_clear ÷ weekly_start（両方が取れる週のみ。取れない週は「-」）。
- share系合計 = share_x + share_line + card_saved（上の週次ログの各週の合算を転記）。
- weekly_* / share系は track の有効化（TODO ☐9）後に埋まる。**X列は告知投稿のアナリティクスから手で転記**するので有効化前でも埋められる。
- 埋まったら一行総括を追記する（例: 「静か系のほうがクリア率は低いが share系は高い → PAGE 5 の SILENT 系素材を先に厚くする」）。判断は数字が両系そろってから。

## イベントが飛ぶ仕組み（現状）

- アプリ内の `track(event, props)` は **`window.plausible` が存在するときだけ**イベントを送る（無ければ完全 no-op）。
- 計測イベント一覧は README の計測表を参照（weekly_start / weekly_clear / weekly_fail / share_x / share_line / card_saved / share_other / export_json / export_csv / notification_optin など。全てに `app_platform` 付き）。
- つまり**アナリティクスのスクリプトを有効化するまで、この表のイベント列は埋まらない**（訪問数すら取れない）。有効化はユーザー作業（TODO ☐9）。

## 有効化の選択肢（TODO ☐9 の詳細）

| 案 | 費用 | 取れるもの | 手順 |
|---|---|---|---|
| **A. Plausible（推奨・イベントが取れる唯一の案）** | 30日無料→有料（月$9〜） | 訪問数＋**全カスタムイベント** | plausible.io でサイト登録（domain: `larai-w.github.io`）→ `web/index.html` のコメントアウトされた script 行を解除 → push |
| B. Cloudflare Web Analytics | 無料 | 訪問数のみ（`track()` イベントは**取れない**） | CF でサイト登録 → beacon の script タグを `web/index.html` に追加（Plausible 行の代わり） |
| C. 何もしない | 0 | ストア統計と手動メモのみ | 20人フェーズの序盤はこれでも回る（全員と直接話せるため） |

> 推奨: **20人フェーズの告知を始める週に A を開始**（トライアル30日を「最初の波」に合わせると費用対効果が最大）。
> どの案でも Cookie レス・個人非特定（アプリ内の計測倫理の記述と整合）。
